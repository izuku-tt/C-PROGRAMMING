/*
SQUARE ROOT FUNTION (Library Function)
*/
#include <stdio.h>
#include <math.h>
int main() {
	int n;
	printf("enter the perfect square number=");
	scanf("%d", &n);
	printf("the square rroot of %d is %.3f", n,sqrt(n));
	return 0;
}
